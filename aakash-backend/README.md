# Aakash Backend

This is the fully structured Express.js backend.

## Structure
- `/models`: Contains User, Course, etc.
- `/routes`: Contains all routes mapped to controllers
- `/controllers`: Contains business logic
- `/middleware`: Auth and role middleware
- `/config`: Third-party specific configuration

## Next Steps
Run `npm run dev` in this directory to start the backend running on port 5000.
